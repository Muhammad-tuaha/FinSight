services:
  - type: web
    name: finsight-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn finsight.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
