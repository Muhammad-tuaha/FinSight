# --- PDF parsing ---
pymupdf>=1.24.0
pdfplumber>=0.11.0

# --- Data validation ---
pydantic>=2.0.0

# --- LLM backends (install the one you use) ---
google-generativeai>=0.7.0   # FREE — Gemini (recommended)
# anthropic>=0.28.0           # PAID — Claude (uncomment if needed)

# --- Firebase Admin SDK ---
firebase-admin>=6.0.0